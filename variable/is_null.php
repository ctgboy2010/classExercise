
<?php
    /*error_reporting(E_ALL);

    $foo = NULL;
    //var_dump(is_null($inexistent), is_null($foo));
    echo is_null($foo)?"Null":"Not an null";*/

    $a=array('array','is','an','array');
    $c='hello? how r u? fine..'
    $b=serialize($a);
    echo $b;
    //$b=serialize($a);
    echo unserialize($c);


?>
